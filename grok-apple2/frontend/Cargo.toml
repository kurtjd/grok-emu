[package]
name = "grok-apple2"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
grok-apple2-core = { path = "../core" }
eframe = { version = "0.35", default-features = false, features = [
    "glow",
    "wayland",
    "x11",
    "default_fonts",
    "accesskit",
] }
cpal = "0.16"
serialport = "4"
